# ═══════════════════════════════════════════════════════════════════════════════
# PhotoTochka — переменные окружения
# ═══════════════════════════════════════════════════════════════════════════════
#
# ДЕВ: скопируйте в .env и работайте через ./photo start
# ПРОД: укажите PHOTO_DEPLOY_HOST и запустите ./photo deploy
#       (остальные переменные сгенерируются автоматически на VDS)
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Frontend (Vite) ──────────────────────────────────────────────────────────
# URL API бэкенда. В dev — пусто (прокси Vite). В prod — пусто (nginx прокси).
VITE_API_URL=
# VITE_API_BASE_PATH=/api
# VITE_DEFAULT_OG_IMAGE=https://example.com/og-default.jpg

# ─── Backend (Django) ─────────────────────────────────────────────────────────
DJANGO_SECRET_KEY=django-insecure-development-key-change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DATABASE_URL=
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# PIP_INDEX_URL=https://pypi.org/simple

# ─── Media, кэш, Celery ───────────────────────────────────────────────────────
# CELERY_BROKER_URL=redis://127.0.0.1:6379/0
# REDIS_URL=redis://127.0.0.1:6379/1
# SHOWCASE_CACHE_SECONDS=0
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=

# ─── Admin seeding ────────────────────────────────────────────────────────────
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=admin-password-change-me

# ─── Деплой на VDS ────────────────────────────────────────────────────────────
# IP-адрес или домен сервера (обязательно для ./photo deploy)
PHOTO_DEPLOY_HOST=ваша-вдс.com
PHOTO_DEPLOY_USER=root
PHOTO_DEPLOY_PATH=/opt/phototochka
